name: CI

on:
  push:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "17 6 * * 1"

permissions:
  contents: read

jobs:
  test:
    name: lint, types, tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    env:
      UV_PYTHON: ${{ matrix.python-version }}
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v8.3.2
      - run: uv sync --extra dev
      - run: make test
      - name: README numbers match the committed tables
        run: make check-readme

  full-pipeline:
    name: real data, R cross-check, artifact consistency
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v8.3.2
      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true
      - run: Rscript -e 'install.packages("survey")'
      - uses: actions/cache@v4
        with:
          path: data/raw
          key: nhanes-2017-raw-${{ hashFiles('data/data_manifest.json') }}
      - run: uv sync --extra dev
      - name: Download, verify hashes, analyze, cross-check against R, regenerate outputs
        run: uv run python -m nhanes_diabetes all
      - name: Committed outputs reproduce (informational)
        continue-on-error: true
        run: git diff --exit-code -- README.md outputs/tables/undiagnosis_estimates.csv outputs/tables/r_python_crosscheck.csv
      - uses: actions/upload-artifact@v4
        with:
          name: outputs
          path: |
            outputs/tables
            outputs/figures
            outputs/run_manifest.json
            dashboard/index.html
          retention-days: 30
